import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ctf-muenster-ui',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
